{% extends 'base.html.twig' %}

{% block title %}
    Nueva Imagen
{% endblock %}

{% block body %}

    <div class="container container-fluid pt-1 mt-3">
        <div class="alert alert-primary d-flex align-items-center justify-content-center mt-sm-5 mb-sm-3" role="alert">
            <div class="row row-cols-1 justify-content-center">
                <div class="col text-center">
                    <div class="h1">
                        Registrar Nueva Imagen
                    </div>
                </div>
                <div class="col d-flex justify-content-center">
                    <a class="btn btn-sm btn-success" href="{{ path('app_imagen_index') }}">
                        Volver a la Galería
                    </a>
                </div>
            </div>
        </div>
        {% for message in app.flashes('alerta') %}
            <div id="alerta" class="alert alert-info">
                {{ message }}
            </div>
        {% endfor %}
        {{ include('imagen/_form.html.twig', {
            'imagen': null
        })}}
        <div class="alert alert-info d-flex align-items-center" role="alert">
            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-info-circle me-2" viewBox="0 0 16 16">
                <path d="M8 15A7 7 0 1 1 8 1a7 7 0 0 1 0 14zm0 1A8 8 0 1 0 8 0a8 8 0 0 0 0 16z"/>
                <path d="m8.93 6.588-2.29.287-.082.38.45.083c.294.07.352.176.288.469l-.738 3.468c-.194.897.105 1.319.808 1.319.545 0 1.178-.252 1.465-.598l.088-.416c-.2.176-.492.246-.686.246-.275 0-.375-.193-.304-.533L8.93 6.588zM9 4.5a1 1 0 1 1-2 0 1 1 0 0 1 2 0z"/>
            </svg>
            <div>
                Un administrador deberá aprobar la imagen, es decir,
                no aparecerá de manera inmediata en la Galería.
            </div>
        </div>
    </div>
    <script>
        const alerta = document.getElementById('alerta');

        setTimeout(function() {
            alerta.style.display = 'none';

            // TODO: Eliminar el mensaje del DOM:
            // alerta.parentNode.removeChild(alerta);
        }, 10000);
    </script>
{% endblock %}
